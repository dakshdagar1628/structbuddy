# Git Workflow Rules

- **Auto-Push**: After completing any code, styling, or configuration changes and verifying them, automatically commit and push the changes to Git (`git push origin main`) without waiting for explicit user prompting.
